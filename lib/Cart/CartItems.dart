class CartItem {
  int id;
  String title;
  int price;
  String image;
  int count;

  CartItem({
     required this.id,
    required this.title,
    required this.price,
    required this.image,
    required this.count,
  });
}